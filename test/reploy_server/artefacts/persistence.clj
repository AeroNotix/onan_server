(ns reploy-server.artefacts.persistence
  (:require [reploy-server.artefacts.persistence :refer :all]
            [clojure.test :refer :all]))


(deftest simple-artefact
  (is false))

(deftest conflicting-artefact
  (is false))

(deftest recursive-dependency
  (is false))
